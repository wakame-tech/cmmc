function f1(n)
begin
  if n = 0 then return 1;
  return n + f2(n - 1);
end

function f2(n)
begin
  if n = 0 then return 1;
  return n * f1(n - 1);
end

var n;
begin
  n := read;
  write f1(n);
  writeln;
end.